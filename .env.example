# Admin Credentials (full access)
# Copy this file to .env.local and change the values
AUTH_USERNAME=admin
AUTH_PASSWORD=changeme

# Read-Only Credentials (optional - can view but not modify)
# Leave empty to disable read-only access
AUTH_READONLY_USERNAME=viewer
AUTH_READONLY_PASSWORD=readonly
